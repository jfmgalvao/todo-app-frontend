import styled from 'styled-components';

export const Button = styled.button`
  padding: 10px 20 px;
  backfound-color: transparent;
  cursos: pointer;
  color: #fff;
  border-bottom: 2px solid rgba(255, 255, 255, 0.2);
`;
